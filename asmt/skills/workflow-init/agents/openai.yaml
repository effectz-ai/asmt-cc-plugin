interface:
  display_name: "Initialize ASMT Workflow"
  short_description: "Configure ASMT for Claude Code and Codex"
  default_prompt: "Use $asmt:workflow-init to configure the ASMT workflow for this repository."

policy:
  allow_implicit_invocation: false
